import Link from 'next/link'; import {PageHero,Shell} from './site-shell';
export function DetailPage({eyebrow,title,intro,fits,flow,note}:{eyebrow:string,title:string,intro:string,fits:string[],flow:string[],note:string}){return <Shell><PageHero eyebrow={eyebrow} title={title} copy={intro}/><section className="detail-split"><div><p className="kicker">A good fit for</p><ul className="check-list">{fits.map(x=><li key={x}>{x}</li>)}</ul></div><div><p className="kicker">How it works</p><ol className="number-list">{flow.map((x,i)=><li key={x}><span>{String(i+1).padStart(2,'0')}</span>{x}</li>)}</ol></div></section><section className="terms-strip"><div><h2>Pricing and delivery</h2><p>Quote-based. Your written agreement will state the rate or fixed fee, payment schedule, deliverables, start conditions, estimated delivery timing, acceptance process, and cancellation terms before payment.</p></div><div><h2>Important note</h2><p>{note}</p></div></section><section className="cta"><p className="kicker light">Next step</p><h2>Share the requirement.</h2><Link className="button inverse" href="/contact">Request a quote <span>↗</span></Link></section></Shell>}
